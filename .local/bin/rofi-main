#!/usr/bin/env sh

# shellcheck disable=SC1090

# Note: if some desktop applications do not show up, make sure that
# the 'NoDisplay' key is false
rofi -show drun -run-shell-command '{terminal} -e zsh -ic "{cmd} && read"'
