#!/usr/bin/env sh

# shellcheck disable=SC1090

# Shutdown and reboot with rofi
bash -i -c "$(echo "logout|reboot|shutdown|emacs_start|emacs_stop|emacs_restart|gnome_keyring_daemon|set_keyboard_layout|vpn-reconnect" | rofi -sep "|" -dmenu)"
