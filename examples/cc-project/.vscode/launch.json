{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build & Debug //:main",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bazel-bin/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "bazel:build-dbg"
    }
  ]
}
